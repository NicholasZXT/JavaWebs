server:
  port: 8100

logging:
  level:
#    root: DEBUG
    root: INFO

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: mysql2022

############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
    # token 名称（同时也是 cookie 名称）
    token-name: satoken
    # token 有效期（单位：秒） 默认30天，-1 代表永久有效
    timeout: 2592000
    # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
    active-timeout: -1
    # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
    is-concurrent: true
    # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
    is-share: false
    # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
    token-style: uuid
    # 是否输出操作日志
    is-log: true


# SpringDoc的配置，配置项参考官方文档 https://springdoc.org/v1/#springdoc-openapi-core-properties
springdoc:
  # 自定义 Swagger UI 路径 (可选)
  swagger-ui:
    path: /swagger-ui.html
    # 启用或禁用 Swagger UI
    # enabled: true

  # OpenAPI 文档路径 (可选)
  api-docs:
    enable: true
    version: openapi_3_0
    path: /v3/api-docs

  # 扫描/排除的包路径 (可选)
  packages-to-scan: pers.zxt.springboot.satoken.controller
#  packages-to-exclude: p1,p2

  # 匹配/排除的接口路径 (可选)
#  paths-to-match: /swagger/basic/**
#  paths-to-exclude: /students/*
